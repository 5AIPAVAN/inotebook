import { createContext } from "react";

// Creating a new Context and export it 
const NoteContext = createContext();

export default NoteContext;

// Can be crated at the beginning of noteState.js
// For better Organization kept it separtate